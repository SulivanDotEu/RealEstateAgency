{% extends '@Admin/property/layout.html.twig' %}

{% block body %}


    <div class="card">
        <div class="header">
            <h4 class="title">Property edition</h4>
            {#<p class="category">#}
                {#You can manage as many center as you want, with the permission of the owner.#}
                {#<br>#}
                {#<a href="{{ path('property_new') }}">Create a new property</a>#}
            {#</p>#}
        </div>
        <div class="content">

            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <div class="footer text-center">
                <input type="submit" value="Edit" class="btn btn-info btn-fill btn-wd"/>

            </div>
            {{ form_end(edit_form) }}

        </div>
    </div>

    {% include '@Admin/property/common_form.html.twig' %}

{% endblock %}
